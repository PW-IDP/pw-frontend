import { makeStyles } from '@mui/styles';

export default makeStyles((theme) => ({
    spinner: {
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        marginTop:'30vh'
    },
}));